{
  "name": "@voix/protocol",
  "private": true,
  "version": "0.0.0",
  "description": "voix wire protocol — shared types between daemon, web UI, and RN clients. Types-only, no build step.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
