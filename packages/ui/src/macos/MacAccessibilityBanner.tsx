/**
 * Web + iOS sibling — Accessibility is a macOS-only concept here, so
 * this renders nothing on non-macOS. Real impl: MacAccessibilityBanner.native.tsx.
 */
export function MacAccessibilityBanner(): null {
  return null;
}
